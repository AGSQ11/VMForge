# Include this snippet in your vmforge server {} to expose ISO_DIR for downloads
# Example: ISO_DIR=/var/lib/vmforge/isos  =>  served under /isos/
location /isos/ {
  alias /var/lib/vmforge/isos/;
  autoindex off;
  # Optional: Restrict to agents subnets, add auth, etc.
}
